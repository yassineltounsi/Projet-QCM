#include "interface.h"

/*
    Fonction principale du programme.
*/
int main(void)
{
    /* Lance le menu principal et laisse l'utilisateur choisir son mode. */
    lancerApplication();

    /* Code de retour 0 : le programme s'est termine normalement. */
    return 0;
}
